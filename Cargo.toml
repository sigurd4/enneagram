[package]
name = "enneagram"
version = "0.1.0"
edition = "2024"

[features]
default = ["blasphemy"]
blasphemy = ["dep:rand"]

[dependencies]
moddef = "0.3.0"
enum-display = "0.2.1"
rand = {version = "0.10.0", optional = true}